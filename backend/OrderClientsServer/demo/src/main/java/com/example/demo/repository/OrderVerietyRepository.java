package com.example.demo.repository;

import com.example.demo.entity.OrderVeriety;
import org.springframework.data.jpa.repository.JpaRepository;

public interface OrderVerietyRepository extends JpaRepository<OrderVeriety, Long> {
}